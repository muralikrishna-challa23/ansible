- name: loops
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: loops install
    ansible.builtin.package:
     name: "{{item}}"
     state: installed
    loop:
    - nginx
    - mysql
    - zip
